To run this:
1)Install Ollama
2)Enable Developer Mode in extensions(browser) and load the folder with the files.
3)Start localhost by typing python fakenews.py in terminal in the compiler u are using for the program.
If it gives an error telling module missing then use pip install module_name to maake it work.
4)Open a news website and click on the extension to check the legitimacy .
